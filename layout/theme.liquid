<!doctype html>
<html>
  <head>
    <title>{{ page_title }}</title>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="description" content="{{ page_description | escape }}">
    <link rel="canonical" href="{{ canonical_url }}">
    <meta name="viewport" content="width=device-width,initial-scale=1,shrink-to-fit=no">
    {{ content_for_header }}
    <!-- Header hook for plugins -->
    {{ '../src/tailwind.css' | asset_url | stylesheet_tag }}
    {{ 'application.css' | asset_url | stylesheet_tag }}
    <script src="{{ 'application.js' | asset_url }}" async></script>
    <script src="https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js" defer></script>
    {% comment %} script_tagはレンタリング時に同期的に処理・パフォーマンスに影響があるという事からサポートされていない（推奨していない）エラーが発生  
      また、script_tagの引数にdeferが渡せないので、上記の記述になった{% endcomment %}
    {% comment %} {{ 'https://unpkg.com/alpinejs@3.x.x/dist/cdn.min.js' | script_tag: defer: true }} {% endcomment %}
  </head>
  <body>
    {%- section 'header' -%}
    <main role="main">
      {{ content_for_layout }}
    </main>
  </body>
</html>
